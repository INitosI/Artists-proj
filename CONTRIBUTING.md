# 🤝 Правила розробки ArtistsHub

## 🎯 Workflow

1. **Створи гілку з назвою задачі:**

   ```bash
   git checkout -b feature/artists-section
   ```

2. **Розробляй функціонал**

3. **Перевір, що все працює:**

   ```bash
   npm run dev
   npm run build
   ```

4. **Створи Pull Request в `main`**

5. **Дочекайся ревью від тімліда**

---

## 📏 Стандарти коду

### HTML

- Використовуй **семантичні теги** (`<header>`, `<section>`, `<article>`)
- Додавай **`alt`** для зображень
- Використовуй **`aria-label`** для кнопок без тексту

### SCSS

- **BEM-нотація:** `.block__element--modifier`
- Використовуй **змінні** з `_variables.scss`
- Використовуй **міксіни** для медіа-запитів

Приклад:

```scss
.artist-card {
  &__image {
    width: 100%;
  }

  &__title {
    color: $primary-color;
  }

  &--featured {
    border: 2px solid $accent-color;
  }
}
```

### JavaScript

- **ES6+** синтаксис
- **Async/await** для асинхронних операцій
- **Деструктуризація** параметрів
- **Стрілкові функції** де можливо

Приклад:

```javascript
const loadArtists = async ({ page = 1, limit = 8 } = {}) => {
  try {
    const { data } = await fetchArtists({ page, limit });
    renderArtists(data);
  } catch (error) {
    console.error(error);
  }
};
```

---

## 🚫 Заборонено

❌ **НЕ використовуй `fetch`** — тільки Axios з `axios-instance.js`  
❌ **НЕ коміть в `main`** — тільки через PR  
❌ **НЕ залишай `console.log`** в продакшені  
❌ **НЕ хардкодь URL API** — використовуй `axios-instance.js`

---

## ✅ Чеклист перед PR

- [ ] Код запускається без помилок (`npm run dev`)
- [ ] Проєкт збирається (`npm run build`)
- [ ] Немає `console.log` в коді
- [ ] Використано BEM для CSS-класів
- [ ] Використано axios-instance для запитів
- [ ] Додано коментарі для складної логіки

---

## 🐛 Як повідомити про баг

1. Відкрий [Issue](https://github.com/INitosI/Artists-proj/issues/new)
2. Опиши проблему
3. Додай скріншот (якщо можливо)
4. Вкажи кроки для відтворення

---

## 📞 Зв'язок

Питання? Пиши в Telegram

```

```
